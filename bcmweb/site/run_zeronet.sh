#!/bin/bash

echo "ERROR DELETE THIS FILE"

# set -Eeuo pipefail
# cd "$(dirname "$0")"

# mkdir -p "$DATA_DIR"
# mkdir -p "$LOG_DIR"
# mkdir -p "$PLUGINS_DIR"
# mkdir -p "$TOR_DIR"

# wait-for-it -t 10 "127.0.0.1:43110"

